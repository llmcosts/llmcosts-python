# Example environment variables for LLM cost tracking callbacks
# Copy this file to .env in your project root and set the actual paths

# SQLite callback configuration
# Directory where the SQLite database will be created (llm_cost_events.db)
SQLITE_CALLBACK_TARGET_PATH=.

# Text callback configuration  
# Directory where the text file will be created (llm_cost_events.jsonl)
TEXT_CALLBACK_TARGET_PATH=. 